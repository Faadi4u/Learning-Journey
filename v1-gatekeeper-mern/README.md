# Project 1: GateKeeper (MERN Baseline)

This directory contains the initial version of the authentication system.
Pending initialization of Server and Client.

---

# ⚙️ GateKeeper API (Server/Backend)

The backend REST API for GateKeeper v1. Built with **Express**, **TypeScript**, and **Mongoose**

## 🔧 Environment Variables

```env
# Server Configuration
PORT=5000

# Database Connection
MONGODB_URI=Your URI

# Security Secrets ( Pending )

# CORS (Frontend URL)
CORS_ORIGIN=http://localhost:5173

